/**
 * @file Visual tone of status indicators shared across the UI.
 */

/**
 * Visual tone of a status indicator (pill, status line, method status).
 */
export enum StatusTone {
    Ok = 'ok',
    Warn = 'warn',
    Pending = 'pending',
    Off = 'off',
}
